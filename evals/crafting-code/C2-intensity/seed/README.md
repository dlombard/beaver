# reporting-tool (seed)

A tiny Python reporting tool (`report.py`) used as the starting point for the
C2-intensity eval. The task asks to add "a configurable plugin system so users can
register custom date-format handlers." The stack is Python — no need to ask.
